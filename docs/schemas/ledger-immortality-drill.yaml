schema_id: ledger-immortality-drill
schema_version: 1
source_doctrine: docs/runbooks/IMMORTALITY_DRILL.md
source_sha256: 055217f9fcbe3c9eb83be255e6c4314b1decf785a3d052c5e192db61596f4815
status: canonical
row_type: immortality_drill_rehearsal_v1

required_fields:
  - schema_version
  - event
  - run_id
  - as_of_utc
  - primary_substrate
  - secondary_substrate
  - status
  - residual_closed
  - residual
  - prev_hash
  - row_hash

allowed_events:
  - immortality_drill_rehearsal_v1
  - immortality_drill_third_party_v1

allowed_statuses:
  - passed
  - failed
  - skipped_with_residual

hashing:
  canonical_json: sort_keys_true_separators_comma_colon
  row_hash_excludes:
    - row_hash

verification:
  writer: scripts/immortality-drill-rehearsal.sh
  third_party_helper: scripts/immortality-drill-third-party.sh
  notes:
    - Rehearsal rows keep LHT-SUBSTRATE-001 open until the annual cadence and warm-secondary preconditions are satisfied.
    - Third-party-machine rows are operator-supplied evidence; the helper prints a row for append-only review rather than writing to the ledger directly.
